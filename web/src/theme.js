import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#d29876',
      contrastText: '#fff',
    },
    secondary: {
      main: '#d27682',
      contrastText: '#fff',
    },
  },
});

export default theme;
